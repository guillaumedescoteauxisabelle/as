#!/bin/bash
goon=1
. .env &> /dev/null || . _env.sh &> /dev/null || goon=0

if [ "$goon" == 1 ]; then
echo "Running container : $dockertag" && \
docker run -it --rm \
	-v $(pwd):/work \
	-v $binroot:/a/bin \
	-e DISPLAY=$DISPLAY \
	$dockertag $1 
else	
echo "Please define dockertag in .env"
fi
