# System common
#
# AST
alias cdast="cd $srcroot/gix-adaptive-style-transfer-2201/bin-gpu;ls -ltr"

#DK Related
alias dkbuild="$binroot/dkbuild"
alias dkbuilduser="$binroot/dkbuilduser"
alias dkpush="$binroot/dkpush"
alias dkrun="$binroot/dkrun "
alias dksvc="$binroot/dkcSvc.sh "
alias dkps="$binroot/dkps"
alias dkinfo="$binroot/dkinfo"
alias dkebash="$binroot/dkebash"
alias dkcrm="$binroot/dkcRemove.sh"
alias dkcls="echo Running container;$binroot/dkcList.sh"
alias dkclsport="echo Running container;$binroot/dkcListPort.sh"
alias dkclsstopped="echo Stopped container;$binroot/dkcListStopped.sh"
alias dkvolbak="$binroot/dkvolbak.sh "

alias dkrun-ubuncompiler="docker run -it --rm -v $(pwd):/work jgwill/ubuntu:compiler bash"1~

# CD to dir
export caishenroot="/c/Caishen/Caishen"
alias cdspider="cd $caishenroot/src/Caishen/Databases/SpiderDbDatabase/gia-mssql"
alias cdcaishenbin="cd $caishenroot/binDebug;ls -ltr"

# COMMON
alias vialias="vi $binroot/etc/bash_aliases_common && . $binroot/etc/bash_aliases_common && (cd $binroot;git commit etc -m chg:etc-alias &>/dev/null && git push&>/dev/null)"
alias vifn="vi $binroot/__fn.sh && . $binroot/__fn.sh && (cd $binroot;git commit __fn.sh -m chg:fn&> /dev/null && git push &>/dev/null)"
alias vienv="vi $binroot/etc/bash_env_common && . $binroot/etc/bash_env_common && (cd $binroot;git commit etc -m chg:etc-env &>/dev/null && git push &>/dev/null)"
alias editalias=vialias
alias bvi="$binroot/bvi.sh "

# RW Related
alias cdrw="cd $srcroot/rwml__adaptive_style_transfer"
alias mckinfo="$binroot/mckinfo.sh"

# Mk Md, Web
alias mkimgmdmm="$binroot/img_mk_mdref_mn__build.sh"

#git
alias gs="git status"
alias gd="git diff "
alias gdb="git diff -b "
#alias gcp="'add_commit_push() { local _msg=\"$1\";shift 1;git add \"$@\" &> /dev/null ; git commit \"$@\" -m $_msg; git push &>/dev/null;}';add_commit_push "

#
#Common
alias greps="$binroot/grepSearch.sh "

alias echomdtableheader='echo "|       |       |       |
|  ---  |  ---  |  ---  |
"'


